# Enum SeismicAccessHoleType 

The type of hole configuration for seismic welds to use. These are pre-defined in AISC manuals, but could be user adjusted in setup.

###### **Namespace**: [DesignData](DesignData.html).[SDS2](DesignData.SDS2.html).[Setup](DesignData.SDS2.Setup.html)

###### **Assembly**: DesignData.SDS2.Setup.dll

##### Syntax

```
public enum SeismicAccessHoleType
```

### [](#fields)Fields 

| Name | Description                                                                                                                          |
| ---- | ------------------------------------------------------------------------------------------------------------------------------------ |
| A    | The type of hole configuration for seismic welds to use. These are pre-defined in AISC manuals, but could be user adjusted in setup. |
| B    | The type of hole configuration for seismic welds to use. These are pre-defined in AISC manuals, but could be user adjusted in setup. |
| C    | The type of hole configuration for seismic welds to use. These are pre-defined in AISC manuals, but could be user adjusted in setup. |
| D    | The type of hole configuration for seismic welds to use. These are pre-defined in AISC manuals, but could be user adjusted in setup. |
| E    | The type of hole configuration for seismic welds to use. These are pre-defined in AISC manuals, but could be user adjusted in setup. |
| F    | The type of hole configuration for seismic welds to use. These are pre-defined in AISC manuals, but could be user adjusted in setup. |
| G    | The type of hole configuration for seismic welds to use. These are pre-defined in AISC manuals, but could be user adjusted in setup. |
| H    | The type of hole configuration for seismic welds to use. These are pre-defined in AISC manuals, but could be user adjusted in setup. |
| I    | The type of hole configuration for seismic welds to use. These are pre-defined in AISC manuals, but could be user adjusted in setup. |
| J    | The type of hole configuration for seismic welds to use. These are pre-defined in AISC manuals, but could be user adjusted in setup. |
| K    | The type of hole configuration for seismic welds to use. These are pre-defined in AISC manuals, but could be user adjusted in setup. |
| L    | The type of hole configuration for seismic welds to use. These are pre-defined in AISC manuals, but could be user adjusted in setup. |
| M    | The type of hole configuration for seismic welds to use. These are pre-defined in AISC manuals, but could be user adjusted in setup. |
| N    | The type of hole configuration for seismic welds to use. These are pre-defined in AISC manuals, but could be user adjusted in setup. |
| None | Do not use a hole configuration for seismic welds                                                                                    |